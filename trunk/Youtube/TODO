* Change the about dialog into an about window

* Check resolution when loading skin

* Add a settings window
	* Make stripping invalid chars configurable.

* Add download to youtube.py
	* Add download-and-play mode
	* Add store to disk mode

* Do something about the ugly user menu handling.
